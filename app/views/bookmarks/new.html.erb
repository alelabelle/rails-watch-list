<%= simple_form_for [@list, @bookmark] do |f| %>
  <%= f.input :movie, collection: Movie.all, include_hidden: false %>
  <%= f.input :comment %>
  <%= f.submit 'Add movie', class: 'btn btn-primary' %>
<% end %>

<%= link_to "Go back to lists", lists_path(@index) %>
